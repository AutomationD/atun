# Integration Tests

This directory should contain integration tests for the project. 
For now there are no integration tests, but we will add them soon.

## Tests Planned
### `atun add`
1. Config via env vars (no atun.toml)
2. Config via survey (no atun.toml)
3. Config via atun.toml (no env vars)
4. Config via atun.toml and env vars
5. Config via atun.toml and survey
